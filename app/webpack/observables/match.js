export const homeScore = (match) => match.map('.home_score')
export const awayScore = (match) => match.map('.away_score')

export const homeName = (match) => match.map('.home_player.name')
export const awayName = (match) => match.map('.away_player.name')

export const homePortrait = (match) => match.map('.home_player.portrait_url')
export const awayPortrait = (match) => match.map('.away_player.portrait_url')
