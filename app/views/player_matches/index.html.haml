.player-match-history.porkbox-light
  %p= link_to "← Back", @player
  %h1= "#{@player.name}'s Match History"
  = render "player_matches/list", matches: @player.matches.finalized.order(finalized_at: :desc)
